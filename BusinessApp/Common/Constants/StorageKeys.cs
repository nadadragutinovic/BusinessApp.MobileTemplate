namespace BusinessApp.Common.Constants;

/// <summary>
/// Keys used for SecureStorage and Preferences.
/// </summary>
public static class StorageKeys
{
    public const string JwtToken = nameof(JwtToken);

    public const string RefreshToken = nameof(RefreshToken);

    public const string UserName = nameof(UserName);

    public const string RememberMe = nameof(RememberMe);

    public const string Theme = nameof(Theme);

    public const string Language = nameof(Language);
}
